# AGENTS

- Specifications for new features will be described in `docs/spec/`
- Favour simplicity in implementation where possible
- Don't add additional dependencies or tools unless they're useful
- Ask before adding any new dependencies or tools
- Commit after every block of work or complete task
- Use Conventional Commits format for commit messages
- Sign commits using an SSH key; if the key isn't loaded, ask me to load it for you
- When committing, list the current AI model in a commit trailer using the format "Assisted-by: AGENT_NAME:MODEL_VERSION"
